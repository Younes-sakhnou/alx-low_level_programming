#include "main.h"
#include<stdio.h>

void _puts(char *str);
/**
 * main - Entry point
 * Return: 0 Always success
 */
int main(void)
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
