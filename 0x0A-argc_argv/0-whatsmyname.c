#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: parameter
 * @argv: parameter
 * Return: 0
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
